import { createContext } from "react";

//создание контекста - исп. функции createContext
//при создании context былинициализирован данными - null
export const AuthorizedContext = createContext({});
